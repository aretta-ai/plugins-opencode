---
name: status
description: Check the status of a proof — trust levels, open obligations, staleness.
---

IMMEDIATELY call the `status` tool from the aretta-proof MCP server. Do NOT explain or add preamble before calling the tool. If the user specifies a proof ID, pass it. Otherwise omit it.

After the tool returns, interpret the result: summarize status, trust levels, and open obligations in a readable format.

$ARGUMENTS
